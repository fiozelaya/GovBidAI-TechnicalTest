const metadata = {
    title: "Author",
    description: "Author page",
  };

function AuthorLayout({children}) {
  
    return (
      <div>
          {children}
      </div>
    );
  }
  
  export default AuthorLayout;
  