const metadata = {
    title: "Genre",
    description: "Genre page",
  };

function GenreLayout({children}) {
  
    return (
      <div>
          {children}
      </div>
    );
  }
  
  export default GenreLayout;
  