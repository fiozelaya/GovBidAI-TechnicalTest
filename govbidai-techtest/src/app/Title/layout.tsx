const metadata = {
    title: "Title",
    description: "Title page",
  };

function TitleLayout({children}) {
  
    return (
      <div>
          {children}
      </div>
    );
  }
  
  export default TitleLayout;
  